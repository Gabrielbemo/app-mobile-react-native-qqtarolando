import React, { version } from 'react';
import { View, Text, StyleSheet } from 'react-native'

export default class NewsDetailPage extends React.Component {

    render() {
        return (
            <View>
                <Text>page de detalhes</Text>
            </View>
        );
    }
}

const styles = StyleSheet.create({
});
